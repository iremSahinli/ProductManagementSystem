@{
    Layout = null;
}
<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Şifre Unutma</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
            background-color: #f4f4f4;
        }

        .forgot-password-container {
            background-color: #fff;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }

            .forgot-password-container label,
            .forgot-password-container input {
                display: block;
                width: 100%;
                margin-bottom: 10px;
            }

        /* Butonları yan yana getirmek için flexbox kullanıldı */
        .button-container {
            display: flex;
            /*gap: 0;  Bitişik olmaları için boşlukları kaldırdım */
            justify-content:center;
        }

        .forgot-password-container button {
            
            font-size: 16px;
            cursor: pointer;
            border-radius: 4px;
            border: none;
            
            width: auto; /* Genişliği yazı kadar ayarla */
            padding: 10px 20px; /* Butonun etrafındaki boşlukları ayarlamak için */
            text-align: center; /* Yazıyı ortala */
        }

            .forgot-password-container button[type="submit"] {
                background-color: #007bff;
                color: white;
            }

                .forgot-password-container button[type="submit"]:hover {
                    background-color: #0056b3;
                }

            /* Back to Login Page butonuna kırmızı renk verdim */
            .forgot-password-container button.back-to-login {
                background-color: red;
                color: white;
            }

                .forgot-password-container button.back-to-login:hover {
                    background-color: darkred;
                }

    </style>
</head>
<body>
    <div class="forgot-password-container">
        <h1>Şifrenizi mi unuttunuz?</h1>
        <p>Lütfen kayıtlı e-posta adresinizi girin, size bir şifre sıfırlama bağlantısı göndereceğiz.</p>
        <form asp-action="ForgotPassword" method="post">
            <label for="email">E-posta adresi:</label>
            <input type="email" id="email" name="Email" placeholder="example@mail.com" required />
            <div class="button-container">
                <button type="submit">Gönder</button>
            </div>
        </form>
    </div>
</body>
</html>
